% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_data_toolkit.R
\name{format_state_covariates}
\alias{format_state_covariates}
\title{Format state-level environmental covariates for NIMBLE}
\usage{
format_state_covariates(sitecodes, yearcodes, matching = FALSE, types = c())
}
\arguments{
\item{sitecodes}{A data frame with columns \code{x1km_grid} and \code{siteID} mapping sites to IDs.}

\item{yearcodes}{A data frame with columns \code{year} and \code{yearID} for temporal matching.}

\item{matching}{Logical. If TRUE, attempts to fill missing environmental records with
the nearest available value.}

\item{types}{A character vector of which covariate types to process. Must be a subset of:
\code{"i"} (spatial), \code{"t"} (temporal), \code{"it"} (spatiotemporal).}
}
\value{
A named list of covariate inputs formatted for inclusion in the NIMBLE model.
}
\description{
Combines and processes spatial, temporal, and spatiotemporal covariate datasets
(climate, landcover, and spatial properties) to match model site and year structures
for use in integrated species distribution models (iSDMs) built in NIMBLE.
}
\details{
The function pulls in pre-processed datasets using dedicated import functions, and
applies \code{prep_nimble_cov()} with \verb{nimblefy_*()} helpers to match covariates to the
expected model dimensions (\code{i}, \code{t}, \code{it}). It also applies centering, scaling, and
optional second-order polynomial transformation.
}
\seealso{
\code{\link{prep_nimble_cov}}, \code{\link{import_climate_rasters}}, \code{\link{import_landcover_dfs}}, \code{\link{import_spat_properties_df}}
}
